var shell = require('electron').shell;
var open = require('open');		

module.exports.open = function(url) {
	shell.openItem(url);
}

module.exports.folder = function(url) {
	var deUrl = decodeURIComponent(url);
	open(deUrl); 
}