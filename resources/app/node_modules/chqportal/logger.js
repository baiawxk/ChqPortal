'use strict';


var log4js = require('log4js');
log4js.configure({
  appenders: [
    { type: 'console' },
    { type: 'file', filename: 'log/app.log', category: 'APP' }
  ]
});

let logger = log4js.getLogger('APP');
module.exports = logger;